
% Choose the language
\usepackage[english]{babel} % Load characters and hyphenation
\usepackage[english=british]{csquotes}	% English quotes

% Load packages for testing
\usepackage{blindtext}
%\usepackage{showframe} % Uncomment to show boxes around the text area, margin, header and footer
%\usepackage{showlabels} % Uncomment to output the content of \label commands to the document where they are used

% Load the bibliography package
\usepackage{kaobiblio}
\addbibresource{bibliography.bib} % Bibliography file

% Load mathematical packages for theorems and related environments
\usepackage{kaotheorems}

% Load the package for hyperreferences
\usepackage{kaorefs}

% Load physics packages (mainly just for bra-ket)
\usepackage{physics, bbold}

% Load pdf package for cover
\usepackage{pdfpages}

% Define missing Unicode characters
\DeclareUnicodeCharacter{221A}{\ensuremath{\surd}} % Unicode square-root symbol "\sqrt{}"

\graphicspath{{images/}{./}} % Paths where images are looked for

% Aliases
\newcommand{\red}[1]{\textcolor{red}{(#1)}}
\newcommand{\Er}{Er$^{3+}$\,}
\newcommand{\Nb}{$^{93}$Nb\,}
\newcommand{\W}{$^{183}$W\,}
\newcommand{\Ca}{CaWO$_4$\,}
\newcommand{\cH}[1]{\hat{\mathcal{H}}_{\text{#1}}}